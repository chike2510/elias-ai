"use client";
import Link from "next/link";import {usePathname} from "next/navigation";import {Bell,BookOpen,FileText,Folder,Globe2,Home,Menu,Mic,Search,Settings2,Sparkles} from "lucide-react";import {useState} from "react";import ModalSheet from "@/components/ModalSheet";
export default function AppShell({children,title}:{children:React.ReactNode;title?:string}){const pathname=usePathname();const [open,setOpen]=useState(false);return <div className="app-shell"><header className="topbar"><button className="icon-btn" onClick={()=>setOpen(true)} aria-label="menu"><Menu size={20}/></button><Link href="/" className="brand"><span className="brand-mark"><Sparkles size={18}/></span><span>ELIAS</span><i/></Link><div className="top-actions">{title&&<span className="top-title">{title}</span>}<button className="icon-btn" aria-label="search"><Search size={18}/></button><button className="icon-btn" aria-label="notifications"><Bell size={18}/></button><span className="avatar">E</span></div></header><div className="app-content">{children}</div><nav className="bottom-nav"><Nav href="/" label="Home" icon={Home} active={pathname==="/"}/><Nav href="/projects" label="Projects" icon={Folder} active={pathname.startsWith("/projects")}/><Link className="assistant-fab" href="/agent" aria-label="Open ELIAS"><Sparkles size={23}/></Link><Nav href="/files" label="Files" icon={FileText} active={pathname.startsWith("/files")}/><Nav href="/studio" label="Studio" icon={Mic} active={pathname.startsWith("/studio")}/></nav>{open&&<ModalSheet onClose={()=>setOpen(false)}><div className="drawer-title"><div><span className="brand-mark"><Sparkles size={18}/></span><strong>ELIAS</strong></div><button onClick={()=>setOpen(false)}>×</button></div><div className="drawer-section"><p>Workspace</p><Link href="/agent" onClick={()=>setOpen(false)}>Autonomous agent</Link><Link href="/research" onClick={()=>setOpen(false)}><Globe2 size={14}/> Research</Link><Link href="/study" onClick={()=>setOpen(false)}><BookOpen size={14}/> Study & documents</Link><Link href="/files" onClick={()=>setOpen(false)}>Artifacts & files</Link></div><div className="drawer-section"><p>System</p><Link href="/studio" onClick={()=>setOpen(false)}><Mic size={14}/> Voice + camera</Link><a href="#settings"><Settings2 size={14}/> Settings</a></div></ModalSheet>}</div>}
function Nav({href,label,icon:Icon,active}:{href:string;label:string;icon:any;active:boolean}){return <Link href={href} className={`nav-item ${active?"active":""}`}><Icon size={18}/><span>{label}</span></Link>}
